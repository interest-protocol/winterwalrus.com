import { Div, Input } from '@stylin.js/elements';
import { FC } from 'react';

const StakeFormField: FC = () => {
  return (
    <Div>
      <Input />
    </Div>
  );
};

export default StakeFormField;
