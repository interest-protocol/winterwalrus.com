import { FC } from 'react';

import { Layout } from '@/components';

import DeFiContent from './defi-content';

const DeFi: FC = () => (
  <Layout>
    <DeFiContent />
  </Layout>
);

export default DeFi;
